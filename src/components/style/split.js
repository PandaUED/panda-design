import { color } from './color';

export default {
  all: `border: 1px solid ${color.split}`,
  top: `box-shadow: 0 -1px 0 0 ${color.split}`,
  bottom: `box-shadow: inset 0 -1px 0 0 ${color.split}`,
  left: `border-left: 1px solid ${color.split}`,
  right: `border-right: 1px solid ${color.split}`,
};
