import { color } from './color';

export default {
  all: `border: solid .5px ${color.split}`,
  top: `border-top: solid .5px ${color.split}`,
  bottom: `border-bottom: solid .5px ${color.split}`,
  left: `border-left: solid .5px${color.split}`,
  right: `border-right: solid .5px ${color.split}`,
};
