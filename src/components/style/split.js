import { color } from './color';

export default {
  all: `border: .5px solid ${color.split}`,
  top: `box-shadow: inset 0 0.5px 0 0 ${color.split}`,
  bottom: `box-shadow: inset 0 -0.5px 0 0 ${color.split}`,
  left: `border-left: .5px solid ${color.split}`,
  right: `border-right: .5px solid ${color.split}`,
};
